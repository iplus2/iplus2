# 2026-05-07 历史记录

- 项目早期完成首次 GitHub 推送，远程仓库后来统一为 `iplus2/iplus2`。
- 当时的网络环境无法稳定使用 HTTPS Git，曾改用 SSH；这属于历史环境信息，不代表当前必须使用 SSH。
- 项目随后已从登录/注册/图片画廊原型继续演进，当前结构应以根目录源码和 `PROJECT.md` 为准。
- `.gitignore` 排除了 OpenClaw 工作文件、任务摘要、部分临时脚本、IDE 文件和 `memory/` 目录。

本记录不保存 SSH key、API key 或其他凭据。
