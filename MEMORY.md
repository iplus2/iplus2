# MEMORY.md - 长期决策与背景

- 项目从登录、注册和图片画廊原型演进为个人网站；早期页面已清理，遗留数据库对象须核实后再处理。
- 持续采用原生多页网站与 Supabase；是否引入框架或构建工具由实际需求决定。
- 个人空间用于即时对话，信箱用于站主与 SVIP 互寄不可撤回的延迟照片，两者使用不同存储权限。
- 历史 WSL 专用推送脚本已移除，后续使用当前环境的普通 Git 命令。

当前功能和技术契约见 `PROJECT.md`，协作约束见 `AGENTS.md`。短期记录仅保存在本地 `memory/`，不在本文件保存任务过程或凭据。
